{
  "name": "ml-project-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev:frontend",
    "dev:backend": "npm --prefix frontend run dev:backend",
    "build": "npm --prefix frontend run build"
  }
}
